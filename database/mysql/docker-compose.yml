# --- TO EXECUTE THIS FILE ---
# 1- Use the following command to create and start containers:
#    docker compose up
#    (use the "-d" argument to run in "detached" mode).
# 2- To stop and remove containers, networks, images, and volumes:
#    docker compose down
#    (use the "-v" argument to remove "volumes" created by docker compose up).
# 3- Browse the DB management tool at http://localhost:8090/ or use http://localhost:8091/
#    to connect to the server with either "root" or "${DB_USER}" user IDs.

# https://www.mysql.com/
# https://hub.docker.com/_/mysql
# https://hub.docker.com/_/adminer/
# https://hub.docker.com/r/phpmyadmin/phpmyadmin/

name: mysql

services:
  mysqldb:
    image: mysql:latest
    hostname: "${DB_HOST_NAME}"
    restart: always
    environment:
      MYSQL_DATABASE: "${MYSQL_DATABASE}"
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${USER_SECRET_KEY:?error}" # Get this password from an environment variable
      MYSQL_ROOT_PASSWORD: "${ADMIN_SECRET_KEY:?error}" # Get this password from an environment variable
    ports:
      # <Port Exposed> : <MySQL Port running inside the container>
      - ${DB_SRV_PORT}:3306
    volumes:
      - database_files:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mysqladmin ping -h localhost -u root -p${ADMIN_SECRET_KEY} || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  adminer:
    image: adminer:latest
    hostname: adminer1
    restart: always
    environment:
      ADMINER_DEFAULT_DB_DRIVER: mysql
      ADMINER_DEFAULT_SERVER: "${DB_HOST_NAME}"
      ADMINER_DESIGN: mvt
      ADMINER_PLUGINS: dump-json dump-xml
    ports:
      - ${WEB_UI_1_PORT}:8080
    depends_on:
      mysqldb:
        condition: service_healthy

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    hostname: myAdmin1
    restart: always
    environment:
      PMA_HOST: mysqldb
      PMA_ARBITRARY: 1
    ports:
      - ${WEB_UI_2_PORT}:80
    depends_on:
      mysqldb:
        condition: service_healthy

# Names our volume
volumes:
  database_files:
